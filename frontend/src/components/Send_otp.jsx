import React from "react";

const Send_otp = () => {
  if (typeof window !== "undefined") {
    console.warn(
      "components/Send_otp.jsx has been deprecated — use landing_page/signup/Signup_page.jsx instead.",
    );
  }
  return null;
};

export default Send_otp;
